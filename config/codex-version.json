{
  "$comment": [
    "Single source of truth for the pinned Codex CLI / app-server version.",
    "Every other pin in the repo must agree with `version`:",
    "  - scripts/download-codex.sh          -> CODEX_VERSION",
    "  - apps/desktop/electron/toolchain-manifest.ts -> PINNED_TOOLCHAIN_VERSIONS.codex",
    "  - docker/Dockerfile                  -> ARG CODEX_CLI_VERSION",
    "tests/unit/version-drift.test.ts enforces that agreement.",
    "",
    "`appServerProtocol.generatedFrom` records the binary version that produced",
    "bridges/codex-bridge/src/app-server/generated. Regenerate with:",
    "  bun scripts/generate-codex-app-server-protocol.ts",
    "and verify in CI with `--check`."
  ],
  "version": "0.145.0",
  "appServerProtocol": {
    "generatedFrom": "0.145.0",
    "outputDir": "bridges/codex-bridge/src/app-server/generated"
  }
}
